
Процедура ПроверкаКонфигурации() Экспорт
	
	Отказ = Ложь;
	мт_ТестированиеКонфигурации_Сервер.ПроверкаКонфигурации(Отказ);
	
	Если Отказ Тогда
		
		ПоказатьПредупреждение(, "Обнаружены ошибки при проверке конфигурации (см. журнал регистрации)");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПоказатьСообщение_ОК(ТекстСообщения) Экспорт
	
	ОткрытьФорму("ОбщаяФорма.мт_Информация_ОК", Новый Структура("ТекстСообщения", ТекстСообщения));
	
КонецПроцедуры

